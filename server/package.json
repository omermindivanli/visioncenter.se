{
  "name": "server",
  "version": "1.0.0",
  "description": "server",
  "main": "server.js",
  "scripts": {
    "client": "cd ../client && yarn start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "author": "omer.mindivanli@gmail.com",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "nodemailer": "^4.4.2",
    "nodemon": "^1.14.11"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
